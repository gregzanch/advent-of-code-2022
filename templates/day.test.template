/// <reference types="jasmine" />

import { part1, part2 } from "../src/day$DAY";
import { readPuzzleInput, readExample } from "../src/util";

describe("Day $DAY", () => {
  it("Part 1 Example", () => {
    const { input, output } = readExample($DAY, 1);
    const res = part1(input); //?
    expect(res.toString()).toBe(output);
  });
  xit("Part 1", () => {
    const input = readPuzzleInput($DAY, 1);
    const res = part1(input); //?
    expect(res).toBeDefined();
  });
  xit("Part 2 Example", () => {
    const { input, output } = readExample($DAY, 2);
    const res = part2(input); //?
    expect(res.toString()).toBe(output);
  });
  xit("Part 2", () => {
    const input = readPuzzleInput($DAY, 2);
    const res = part2(input); //?
    expect(res).toBeDefined();
  });
});
